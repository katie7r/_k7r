<% flash.each do |flash_type, message| %>
  <div class='alert <%= bootstrap_class_for flash_type %> fade in'>
    <div class='container'>
      <button type='button' class='close' data-dismiss='alert' aria-label='Close'><span aria-hidden='true'>&times;</span></button>
      <%= message %>
    </div>
  </div>
<% end %>
